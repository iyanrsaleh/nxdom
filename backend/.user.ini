; PHP-FPM per-directory settings (Nginx / Apache + PHP-FPM)
; File ini dibaca otomatis oleh PHP-FPM setiap user_ini.cache_ttl detik (default 300s)

upload_max_filesize = 512M
post_max_size       = 512M
memory_limit        = 512M
max_execution_time  = 300
max_input_time      = 300
